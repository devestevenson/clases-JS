const json = `{"nombre":"Jhon","edad":25,"habilidades":["volar","rayosX","correr"]}`;

const objJSON=JSON.parse(json);
console.table(objJSON);

let modo = document.querySelector('.card')
let cardMode = document.querySelector('.cardmode')

modo.addEventListener('click' () => {
    
})